<h1>posts#index</h1>
<style>
    .fc-event-main::after {
    content: "";
    position: absolute;
    top: 0;
    right: 0;
    bottom: 0;
    width: <%= 100-50 %>%;
    background-color: red;
    z-index: 1; /* Ensure the overlay is above the green background */
    }
</style>
<% if user_signed_in? %>    
    <%= link_to 'ログアウト', destroy_user_session_path, method: :delete %>
    <%= link_to "マイページへ", user_path(current_user.id) %>
<% else %>
    <%= link_to 'ログイン', new_user_session_path  %>
<% end %>
<% if user_signed_in? %>
    <%= link_to "新規投稿へ", new_post_path %>
<% end %>
<div id='calendar'></div>
